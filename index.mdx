---
title: "Spring Cloud Microservices: Production-Ready Java Platform"
sidebarTitle: "Home"
description: "A microservices platform built with Spring Boot 3.5, Spring Cloud 2025, OAuth2/JWT auth, and Kubernetes orchestration for managing users and courses."
---

Spring Cloud Microservices is a fully containerized, Kubernetes-native platform composed of four independent Spring Boot 3.5 services. It handles user and course management with centralized OAuth2 authentication, service-to-service communication via OpenFeign, automatic horizontal scaling, and structured log observability through the ELK stack.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Run the full stack locally with Docker Compose in minutes
  </Card>
  <Card title="Architecture" icon="diagram-project" href="/architecture">
    Understand how the four services connect and communicate
  </Card>
  <Card title="Kubernetes Deployment" icon="dharmachakra" href="/deployment/kubernetes-setup">
    Deploy to Minikube or any Kubernetes cluster step-by-step
  </Card>
  <Card title="API Reference" icon="code" href="/api/usuarios/list-users">
    Explore all Users and Courses REST endpoints
  </Card>
</CardGroup>

## What's inside

The platform consists of four microservices, each with its own responsibility and database:

<CardGroup cols={2}>
  <Card title="msvc-auth" icon="lock" href="/services/msvc-auth">
    OAuth2 Authorization Server — issues JWT tokens on port 9000
  </Card>
  <Card title="msvc-usuarios" icon="users" href="/services/msvc-usuarios">
    Users REST API backed by MySQL 8, runs on port 8001
  </Card>
  <Card title="msvc-cursos" icon="book" href="/services/msvc-cursos">
    Courses REST API backed by PostgreSQL 14, runs on port 8002
  </Card>
  <Card title="msvc-gateway" icon="network-wired" href="/services/msvc-gateway">
    Spring Cloud Gateway — single entry point on port 8090
  </Card>
</CardGroup>

## Get up and running

<Steps>
  <Step title="Clone the repository">
    ```bash
    git clone https://github.com/Miguel-Rodriguez15/msvc.git
    cd msvc
    ```
  </Step>
  <Step title="Start databases and services with Docker Compose">
    ```bash
    docker compose up -d
    ```
    This starts MySQL 8, PostgreSQL 14, `msvc-usuarios` (port 8001), and `msvc-cursos` (port 8002).
  </Step>
  <Step title="Verify services are healthy">
    ```bash
    curl http://localhost:8001/actuator/health
    curl http://localhost:8002/actuator/health
    ```
  </Step>
  <Step title="Deploy to Kubernetes for the full OAuth2 flow">
    For `msvc-auth` and `msvc-gateway`, see the [Kubernetes setup guide](/deployment/kubernetes-setup).
  </Step>
</Steps>

## Key capabilities

<CardGroup cols={2}>
  <Card title="OAuth2 + JWT" icon="shield-halved" href="/security/oauth2-overview">
    Authorization Code flow with PKCE — scoped tokens for `read` and `write` access
  </Card>
  <Card title="Auto-scaling" icon="arrows-up-down" href="/deployment/autoscaling-ingress">
    HPA scales pods to 5 replicas at 50% CPU; NGINX Ingress enforces rate limits
  </Card>
  <Card title="ELK Observability" icon="magnifying-glass-chart" href="/deployment/elk-stack">
    Structured JSON logs shipped to Logstash, indexed in Elasticsearch by service and date
  </Card>
  <Card title="Environment Variables" icon="sliders" href="/deployment/environment-variables">
    Every configurable value documented with defaults for all four services
  </Card>
</CardGroup>
